<?php
// routes/channels.php
// use Illuminate\Support\Facades\Broadcast;

// Broadcast::channel('interpreter.{userId}', function ($user, $userId) {
//     return (int) $user->id === (int) $userId;
// });